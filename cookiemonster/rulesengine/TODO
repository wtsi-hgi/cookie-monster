How can new rules be added?

How can the reprocessing of data be triggered?

How can the changes between file updates be assessed?

Logic to ensure only the latest update to a file is set to be processed by the data manager

Public interface on data manager to allow changes to the state of the work (complete(x), re-process(path), re-process-all())

Public interface on data manager to allow get all previous changes to file x
